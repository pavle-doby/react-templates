# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting DEVELOPMENT
on:
  push:
    branches:
      - development
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd frontend-spa && npm install && npm run build:dev
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE INIT HOSTING:GITHUB }}'
          projectId: PROJECT_ID
          channelId: live
          target: development
          ## entryPoint: frontend-spa > ONLY IF THE FOLDER IS NESTED
